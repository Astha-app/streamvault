[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install --workspace=server --workspace=shared"]

[phases.build]
cmds = ["npm run build --workspace=server"]

[start]
cmd = "npm run start --workspace=server"
